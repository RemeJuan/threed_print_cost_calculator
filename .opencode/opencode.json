{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "firebase": {
      "enabled": true,
      "type": "local",
      "command": ["npx", "-y", "firebase-tools@latest", "mcp", "--only", "core,crashlytics"]
    }
  }
}
