# Interface settings semantics

ClickUp Task: 86cajy1te

## Note

- Interface summary text now comes from `InterfaceSettingsModel.isDefaultView` / `isCustomView`.
- Settings surfaces show `Default view` when all optional elements are visible, else `Custom view`.
- Leaf fallback models removed; resolve once upstream.
